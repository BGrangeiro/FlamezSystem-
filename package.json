{
  "name": "sistema-flamez",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "check": "node scripts/check.js",
    "build": "npm run check",
    "test": "node --test",
    "credentials": "node scripts/credentials.js",
    "backup": "node scripts/backup.js",
    "backup:verify": "node scripts/backup.js verify",
    "preflight": "node scripts/preflight.js",
    "db:migrate": "node scripts/migrate-sqlite.js",
    "setup:production": "node scripts/setup-production.js",
    "hosting:package": "node scripts/prepare-hosting.js",
    "restore": "node scripts/backup.js restore"
  },
  "engines": {
    "node": ">=22.16.0"
  },
  "description": "Gestão de produtos, produção, estoque e custos para impressão 3D",
  "dependencies": {
    "mqtt": "^5.15.2"
  }
}
